#ifndef __STANDART_CONVERTS_H
#define __STANDART_CONVERTS_H

#include "convertio.h"
#include <inttypes.h>

GEN_IO_HEADER(int)

GEN_IO_HEADER(long)

GEN_IO_HEADER(int64_t)

GEN_IO_HEADER(int32_t)

GEN_IO_HEADER(float)

GEN_IO_HEADER(double)

GEN_IO_HEADER(char)

#endif
